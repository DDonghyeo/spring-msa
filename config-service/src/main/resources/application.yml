server:
  port: 8888

spring:
  profiles:
    active: prod
  application:
    name: config-service
  rabbitmq:
    host: 127.0.0.1
    port: 5672
    username: guest
    password: guest
  cloud:
    config:
      server:
        git:
          uri: https://github.com/DDonghyeo/spring-msa-yml
#          search-paths: yml/**
#          default-label: main
