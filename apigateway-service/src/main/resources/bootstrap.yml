spring:
  cloud:
    config:
      uri: http://localhost:8888
      name: apigateway
  rabbitmq:
    host: 127.0.0.1
    port: 5672
    username: guest
    password: guest

management:
  endpoints:
    web:
      exposure:
        include: busrefresh, busenv